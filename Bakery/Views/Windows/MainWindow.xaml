<Window x:Class="Bakery.Views.Windows.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bakery.Views.Workspaces"
        xmlns:views="clr-namespace:Bakery.Views.Workspaces"
        xmlns:viewmodels="clr-namespace:Bakery.ViewModels"
        mc:Ignorable="d" d:Width="800" d:Height="450"
        Title="Основная страница"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <ResourceDictionary Source="../../Resources/ViewResources/MainWindow/MainWindowViewResources.xaml"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition
                Width="Auto"/>
            <ColumnDefinition
                Width="*"/>
        </Grid.ColumnDefinitions>

        <Border
            Grid.Row="0"
            Grid.Column="0"
            BorderBrush="Gray"
            Background="WhiteSmoke"
            BorderThickness="1"
            CornerRadius="5"
            Margin="5">
            <StackPanel
                Margin="5"
                Background="WhiteSmoke">
                <Border
                    BorderBrush="RoyalBlue"
                    Background="RoyalBlue"
                    Padding="5"
                    CornerRadius="2.5">
                    <TextBlock
                        Text="Навигация"
                        Background="RoyalBlue"
                        Foreground="White"
                        HorizontalAlignment="Center"/>
                </Border>

                <ItemsControl
                    ItemsSource="{Binding NavigationCommands}"
                    ItemTemplate="{Binding Source={StaticResource CommandHyperlinkTemplate}}">
                </ItemsControl>
            </StackPanel>
        </Border>

        <Border
            Grid.Row="0"
            Grid.Column="1"
            BorderBrush="Gray"
            Background="WhiteSmoke"
            BorderThickness="1"
            CornerRadius="5"
            Margin="0,5,5,5">
            <StackPanel>
                <StackPanel
                    Margin="5"
                    Background="WhiteSmoke">
                    <Border
                        BorderBrush="RoyalBlue"
                        Background="RoyalBlue"
                        Padding="5"
                        CornerRadius="2.5">
                        <TextBlock
                            Text="Рабочее пространство"
                            Background="RoyalBlue"
                            Foreground="White"
                            HorizontalAlignment="Center"/>
                    </Border>
                </StackPanel>

                <TabControl
                    x:Name="WorkspacesTabControl"
                    ItemsSource="{Binding Workspaces}"
                    ItemTemplate="{Binding Source={StaticResource ClosableTabItemTemplate}}"
                    SelectedIndex="{Binding SelectedWorkspaceIndex}"
                    Margin="5,0,5,5">
                    <TabControl.Style>
                        <Style TargetType="TabControl">
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SelectedWorkspaceIndex}"
                                             Value="-1">
                                    <Setter
                                        Property="Visibility"
                                        Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TabControl.Style>
                </TabControl>
            </StackPanel>
        </Border>
    </Grid>
</Window>
